---
title: 'Setting Up Tailwind CSS in React'
publishedAt: '2020-07-15'
summary: 'Heres is how to setup Tailwind CSS in React'
---


### Setting Up Tailwind CSS in React
Start by creating a new React project with Create React App v5.0+ if you don't have one already set up.

```bash
npx create-react-app my-project
cd my-project
```
Install TailwindCSS and its dependencies.

```bash
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```
---

<Image
  alt={`Norway`}
  src={`/blog/scss/banner.jpg`}
  width={1501 / 2}
  height={712 / 2}
  priority
/>

#### Get started with Tailwind CSS

Tailwind CSS works by scanning all of your HTML files, JavaScript components, and any other templates for class names, generating the corresponding styles and then writing them to a static CSS file.

It's fast, flexible, and reliable — with zero-runtime.